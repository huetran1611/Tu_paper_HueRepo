name: Round 2 parameter grid

on:
  workflow_dispatch:
    inputs:
      num_jobs:
        description: "Number of solve jobs. Round 2 has 324 runs; default 250 gives 74 jobs x 2 runs and 176 jobs x 1 run."
        required: true
        default: "250"
      time_limit_sec:
        description: "Time limit passed to each solver run."
        required: true
        default: "1800"

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  INSTANCE_PATH: instance_time_dependent/100.10.1.txt

jobs:
  prepare:
    name: Prepare Round 2 shards
    runs-on: ubuntu-latest
    outputs:
      experiment_matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v5

      - name: Generate Round 2 shard matrix
        id: matrix
        shell: bash
        run: |
          matrix="$(python3 scripts/round2_make_matrix.py --num-jobs '${{ inputs.num_jobs }}')"
          export MATRIX_JSON="${matrix}"
          python3 - <<'PYEOF'
          import json
          import os
          matrix = json.loads(os.environ["MATRIX_JSON"])
          jobs = len(matrix)
          runs = sum(item["task_count"] for item in matrix)
          if jobs > 254:
              raise SystemExit(f"Matrix has {jobs} solve jobs; keep <=254 to leave room for prepare and aggregate jobs")
          print(f"Prepared {runs} solver runs across {jobs} jobs")
          PYEOF
          echo "matrix=${matrix}" >> "$GITHUB_OUTPUT"

  solve:
    name: Round 2 shard ${{ matrix.shard_id }} (${{ matrix.task_count }} runs)
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 360
    strategy:
      fail-fast: false
      max-parallel: 16
      matrix:
        include: ${{ fromJson(needs.prepare.outputs.experiment_matrix) }}
    steps:
      - uses: actions/checkout@v5

      - name: Build solver
        run: |
          mkdir -p build
          g++ -O3 -std=c++20 src_v2/tabubu_TimeDependent_v2.cpp -o build/tabubu_TimeDependent_v2

      - name: Run shard
        shell: bash
        run: |
          mkdir -p round2_shard_${{ matrix.shard_id }}
          printf '%s' '${{ matrix.tasks_json }}' > tasks.json
          python3 scripts/round2_run_shard.py \
            --binary build/tabubu_TimeDependent_v2 \
            --instance "$INSTANCE_PATH" \
            --tasks-json tasks.json \
            --out-dir round2_shard_${{ matrix.shard_id }} \
            --time-limit-sec '${{ inputs.time_limit_sec }}' \
            --job-task-timeout-sec "$(( ${{ inputs.time_limit_sec }} + 300 ))"

      - name: Upload shard results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: round2-shard-${{ matrix.shard_id }}
          path: round2_shard_${{ matrix.shard_id }}/
          if-no-files-found: warn
          retention-days: 14

  aggregate:
    name: Aggregate Round 2 results
    needs: solve
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - name: Download shard artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: round2-shard-*
          path: collected_round2
          merge-multiple: false

      - name: Aggregate and choose best config
        run: |
          python3 scripts/round2_aggregate.py collected_round2 \
            --out-dir round2_aggregate \
            --instance "$INSTANCE_PATH" \
            --strict

      - name: Upload Round 2 aggregate
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: round2-aggregate
          path: round2_aggregate/
          if-no-files-found: error
          retention-days: 30
