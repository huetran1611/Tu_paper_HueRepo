name: Neighborhood selection 200 - adaptive g2, g3, g4

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  experiment:
    uses: ./.github/workflows/neighborhood-selection-200-three-methods-reusable.yml
    with:
      group_id: group-2
      methods_json: >-
        [{"strategy":"adaptive","gamma_set":"g2","gamma":[0.5,0.3,0.1,0.3]},{"strategy":"adaptive","gamma_set":"g3","gamma":[0.3,0.2,0.1,0.6]},{"strategy":"adaptive","gamma_set":"g4","gamma":[0.5,0.3,0.1,0.6]}]
